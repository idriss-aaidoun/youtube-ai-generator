# ── Web Framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Base de données (SQLite via SQLAlchemy) ───────────────────
sqlalchemy==2.0.30

# ── Configuration ────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── HTTP Client ──────────────────────────────────────────────
httpx==0.27.0
requests==2.31.0

# ── IA — LLM Orchestration (LangChain + Ollama) ──────────────
langchain==0.2.16
langchain-community==0.2.16
langchain-core==0.2.38

# ── IA — Vidéo & Montage (MoviePy + FFmpeg) ──────────────────
moviepy==1.0.3
imageio==2.34.1
imageio-ffmpeg==0.5.1
decorator==4.4.2
tqdm==4.66.4

# ── IA — Images & Miniatures (Pillow) ────────────────────────
Pillow==10.3.0

# ── Utilitaires ──────────────────────────────────────────────
numpy==1.26.4

# NOTA : torch + openai-whisper sont installés séparément dans le Dockerfile
# pour forcer la version CPU et éviter le téléchargement CUDA (~2 Go inutiles)
